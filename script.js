//your JS code here. If required.
const element = document.createElement("p");
element.innerText = `You are using ${window.navigator.appName} version ${window.navigator.appVersion}`;

document.body.append(element);